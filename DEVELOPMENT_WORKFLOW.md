# The Antigravity Agent-First Development Workflow

Welcome to the Quake-GANT orchestrator! To guarantee codebase flawlessly scales around the 1996 idTech framework without risking structural damage, this entire environment operates mathematically under a strict **A.I. Agent-First Paradigm**.

---

## 1. The Workflow Dynamic

Every operational action uniquely executes across a tightly constrained pair-programming synergy:

1. **You: The Top-Level Director**  
   You supply the visionary architectural directives, deployment decisions, and strategic insight. You do not touch the engine C-code or the Python orchestrator layers manually.
2. **Antigravity: The Principal Engineer**  
   The AI (Antigravity) acts locally and autonomously to execute your insight. Antigravity writes the raw C and Python code, verifies structural algorithmic compliance across legacy configurations, actively rewrites the relevant Markdown documentation dynamically, explicitly stages the payload natively, and independently executes the formal autonomous `git commit` loop!

---

## 2. Orchestrator Component Expansion (Web C2)

When building new real-time Web C2 interfaces, asynchronous API polling mechanics, or leaderboard visualization dashboards:
- Provide your high-level UI/UX directives natively to Antigravity (e.g., *"Build me a new live statistics view in our web UI"*).
- Antigravity will traverse the `/web` interface safely decoupled from the engine submodules, draft the Python logic, map it to the frontend via modern glassmorphism CSS aesthetics, and lock-in the documentation tests independently.

---

## 3. Upstream Engine Fixes (Sovereign C-Code)

We strictly enforce **Zero Structural Bleed**. The native C compilation boundaries of `mvdsv`, `ktx`, and `ezquake` are strictly off limits to native repository tampering.

If a severe legacy engine bug requires a native patch inside our environment:
- Explicitly instruct Antigravity to physically assume the role of Principal C-Engineer.
- Antigravity will establish an entirely native, completely severed `C-Code Workspace` explicitly shielded mathematically away from the core `quake-alpha` orchestrator repository structure. This actively avoids natively crashing the Architectural Sentinel.
- Within this isolated directory context, Antigravity will dynamically map Quake's legacy Memory constraints (like engineering `aligned_alloc` structures for 64-byte AVX boundaries), draft the exact GitHub Fork configuration, explicitly formulate the bugfix string, and directly submit the verified Pull Request autonomously to the official QW-Group.
- Once merged upstream by the maintainer group, your nightly GitLab orchestration pipeline (`03:00` automation trigger) will automagically pull your verified patch natively for deployment down directly across your Castor configurations without manual tracking pipelines!
